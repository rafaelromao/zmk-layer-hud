# runtime
run/
build/
__pycache__/
*.pyc
.DS_Store

# local dumps of the keymap message (python3 host/keymap.py --dump > hud/keymap.json for browser tests)
hud/keymap.json
hud/keymap.js

# editor/agent local config
.claude/

# python virtualenv (make venv)
.venv/

hud/keymap-*.json
